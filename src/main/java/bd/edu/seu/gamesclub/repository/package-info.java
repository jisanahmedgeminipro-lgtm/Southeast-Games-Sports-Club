/**
 * Persistence layer (Repository pattern).
 *
 * <p>Spring Data JPA repository interfaces. Each aggregate root gets its own
 * repository, abstracting data access behind a clean, testable contract.
 */
package bd.edu.seu.gamesclub.repository;
