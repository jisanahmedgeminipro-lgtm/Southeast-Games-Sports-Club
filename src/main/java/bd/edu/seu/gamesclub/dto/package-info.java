/**
 * Data Transfer Objects and form-backing beans.
 *
 * <p>DTOs carry data between the presentation and service layers and decouple
 * the web layer from the persistence entities. Bean Validation
 * ({@code jakarta.validation}) annotations are declared here so user input is
 * validated before it ever reaches the domain model.
 */
package bd.edu.seu.gamesclub.dto;
