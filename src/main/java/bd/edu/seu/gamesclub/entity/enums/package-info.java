/**
 * Enumerated types used by the domain entities.
 *
 * <p>All enums are persisted as {@code STRING} (via {@code @Enumerated(EnumType.STRING)})
 * so the database stores human-readable, order-independent values that match the
 * {@code CHECK} constraints defined in the schema.
 */
package bd.edu.seu.gamesclub.entity.enums;
