/**
 * Business / service layer (interfaces).
 *
 * <p>Defines use-case contracts (registration, OTP, authentication, membership,
 * CRUD for content modules, email dispatch). Implementations live in
 * {@code bd.edu.seu.gamesclub.service.impl}, following dependency-inversion so
 * controllers depend on abstractions, not concrete classes.
 */
package bd.edu.seu.gamesclub.service;
