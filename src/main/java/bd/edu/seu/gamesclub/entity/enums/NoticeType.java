package bd.edu.seu.gamesclub.entity.enums;

/** Category of a notice-board entry ({@code notices.notice_type}). Stored as {@code STRING}. */
public enum NoticeType {
    PRACTICE,
    TOURNAMENT,
    HOLIDAY,
    GENERAL
}
