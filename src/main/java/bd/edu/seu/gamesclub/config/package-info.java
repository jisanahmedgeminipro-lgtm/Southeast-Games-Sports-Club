/**
 * Application configuration beans.
 *
 * <p>Holds Spring {@code @Configuration} classes that are <em>not</em> security
 * related: web MVC customizations, static resource / upload handlers, mail
 * sender setup, async &amp; scheduling executors, and {@code @ConfigurationProperties}
 * binding for the {@code app.*} settings.
 */
package bd.edu.seu.gamesclub.config;
