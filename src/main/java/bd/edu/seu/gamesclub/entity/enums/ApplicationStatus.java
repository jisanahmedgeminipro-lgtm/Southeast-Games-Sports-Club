package bd.edu.seu.gamesclub.entity.enums;

/**
 * Status of a student's membership application
 * ({@code membership_applications.status}). Stored as {@code STRING}.
 */
public enum ApplicationStatus {
    PENDING,
    APPROVED,
    REJECTED,
    WITHDRAWN
}
